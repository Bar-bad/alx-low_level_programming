#include "main.h"
/**
 * add - function that adda two integers and returns  the result
 * @a: int to be added
 * @b: int to be added
 *
 * Return: sum of the two integers, `a` and `b`
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
