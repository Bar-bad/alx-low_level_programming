#include "main.h"
/**
 * set_string - function that sets the value o a pointer to a char
 * @s: a pointer to a pointer
 * @to: a pointer
 *
 * Return: Always (0)
 */
void set_string(char **s, char *to)
{
	*s = to;
}
